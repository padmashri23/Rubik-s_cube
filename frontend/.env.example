# Base URL of the CubeGuide backend API (used by the Vite dev proxy).
# The app works fully offline without this; it's only needed if you wire the
# frontend to the backend solve/stats endpoints.
VITE_API_URL=http://localhost:5000
