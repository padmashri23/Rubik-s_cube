# Port the API listens on (default 5000).
PORT=5000
# --- future use -------------------------------------------------------------
# Database connection (when persistence is added):
# DATABASE_URL=postgresql://user:password@localhost:5432/cubeguide
# Auth (Supabase / Firebase) keys, etc.
